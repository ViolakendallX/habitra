# Base URL of the Node.js API.
# In development, the Vite proxy forwards /api/* to http://localhost:4000,
# so the default `/api` works without any .env file. Override here for
# production or non-proxied environments.
VITE_API_BASE_URL=/api
